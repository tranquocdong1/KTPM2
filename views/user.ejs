<!-- <main class="p-6 bg-gray-100 min-h-screen">
    <h1 class="text-3xl font-bold text-center text-gray-800 mb-8">User Management</h1>

    <!-- Search Form -->
    <form class="max-w-md mx-auto mb-6">
        <div class="relative">
            <div class="absolute inset-y-0 left-0 flex items-center pl-3 pointer-events-none">
                
            </div>
            <input 
                value="<%= q %>" 
                name="q" 
                type="search" 
                id="default-search" 
                class="block w-full p-4 pl-10 text-sm text-gray-900 border border-gray-300 rounded-lg bg-white shadow-sm focus:ring-blue-500 focus:border-blue-500 dark:bg-gray-700 dark:border-gray-600 dark:placeholder-gray-400 dark:text-white dark:focus:ring-blue-500 dark:focus:border-blue-500" 
                placeholder="Search by name or email..." 
                required 
            />
            <button 
                type="submit" 
                class="absolute right-2.5 bottom-2.5 bg-blue-600 text-white font-medium rounded-lg text-sm px-4 py-2 hover:bg-blue-700 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-700 dark:hover:bg-blue-800 dark:focus:ring-blue-800">
                Search
            </button>
        </div>
    </form>

    <!-- Create Button -->
    <div class="text-center mb-6">
        <a 
            href="/users/new" 
            class="inline-block bg-green-600 text-white font-medium rounded-lg text-sm px-5 py-2.5 shadow hover:bg-green-700 focus:ring-4 focus:outline-none focus:ring-green-300 dark:bg-green-700 dark:hover:bg-green-800 dark:focus:ring-green-800">
            + Create User
        </a>
    </div>

    <!-- User Table -->
    <div class="relative overflow-x-auto shadow-md sm:rounded-lg bg-white dark:bg-gray-800">
        <table class="w-full text-sm text-left text-gray-700 dark:text-gray-400">
            <thead class="text-xs uppercase bg-gray-50 dark:bg-gray-700 dark:text-gray-400">
                <tr>
                    <th scope="col" class="px-6 py-3">Name</th>
                    <th scope="col" class="px-6 py-3">Email</th>
                    <th scope="col" class="px-6 py-3">Age</th>
                    <th scope="col" class="px-6 py-3">Role</th>
                    <th scope="col" class="px-6 py-3">Action</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach((user) => { %>
                <tr class="odd:bg-white even:bg-gray-50 dark:odd:bg-gray-900 dark:even:bg-gray-800 border-b dark:border-gray-700">
                    <th scope="row" class="px-6 py-4 font-medium text-gray-900 dark:text-white">
                        <%= user.name %>
                    </th>
                    <td class="px-6 py-4"><%= user.email %></td>
                    <td class="px-6 py-4"><%= user.age %></td>
                    <td class="px-6 py-4"><%= user.role %></td>
                    <td class="px-6 py-4 flex space-x-4">
                        <a 
                            href="#" 
                            class="text-blue-600 hover:underline dark:text-blue-400">
                            Edit
                        </a>
                        <form method="post" action="/users/delete/<%= user.id %>" class="inline">
                            <input type="hidden" name="_method" value="DELETE">
                            <button 
                                type="submit" 
                                class="text-red-600 hover:underline dark:text-red-400">
                                Delete
                            </button>
                        </form>
                    </td>
                </tr>
                <% }) %>
            </tbody>
        </table>
    </div>
</main> -->
